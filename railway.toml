[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar backend/target/kavach-backend-*.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
