package com.kavach.alerts.entity;

public enum RuleType {
    SCREEN_TIME_EXCEEDED,
    APP_USAGE_EXCEEDED,
    CATEGORY_USAGE_EXCEEDED,
    LATE_NIGHT_USAGE,
    BLOCKED_APP_ATTEMPT,
    FOCUS_MODE_BROKEN
}
